{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "npm install && lerna bootstrap --ignore-prepublish",
    "clean": "lerna clean --yes && rm -rf node_modules",
    "serve": "lerna run serve --parallel",
    "build": "lerna run build --parallel",
    "copy": "rimraf docs && node ./copy.js",
    "build:example": "npm run build && npm run copy"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "recursive-copy": "^2.0.9"
  },
  "license": "MIT"
}
